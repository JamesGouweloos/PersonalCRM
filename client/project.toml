# Cloud Native Buildpacks project configuration
# This helps Firebase App Hosting detect and build the Next.js app correctly

[build]
  # Specify Node.js version (must match .nvmrc)
  [[build.env]]
    name = "NODE_VERSION"
    value = "18"

